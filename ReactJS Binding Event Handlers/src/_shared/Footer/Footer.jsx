import React from 'react';
//import { Test } from './Footer.styles';

const Footer = (props) => (
  <div style={{ textAlign: 'center'}} className="FooterWrapper">
    Footer
  </div>
);

Footer.propTypes = {
  // bla: PropTypes.string,
};

Footer.defaultProps = {
  // bla: 'test',
};

export default Footer;
