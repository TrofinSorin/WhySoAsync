import React from 'react';

const Home = (props) => (
  <div className="HomeWrapper">
    Home
  </div>
);

Home.propTypes = {
  // bla: PropTypes.string,
};

Home.defaultProps = {
  // bla: 'test',
};

export default Home;
