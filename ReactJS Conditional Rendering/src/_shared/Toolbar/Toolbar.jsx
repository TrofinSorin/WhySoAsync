import React from 'react';
//import { Test } from './Toolbar.styles';

const Toolbar = (props) => (
  <div style={{ textAlign: 'center'}} className="ToolbarWrapper">
    Toolbar
  </div>
);

Toolbar.propTypes = {
  // bla: PropTypes.string,
};

Toolbar.defaultProps = {
  // bla: 'test',
};

export default Toolbar;
